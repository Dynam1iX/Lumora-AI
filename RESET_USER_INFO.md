# 🔄 Сброс информации пользователя

## Проблема

Если вы ввели неверный email или имя при первом запуске, frontend сохранил их в браузере.

Вы видите ошибку:
```
❌ Ошибка обработки запроса
Проблема валидации данных
```

Или ошибку 422 в логах backend.

---

## ✅ Решение (1 минута)

### Способ 1: Через DevTools (рекомендуется)

1. Откройте frontend: http://localhost:5173

2. Нажмите `F12` (или `Ctrl+Shift+I`) чтобы открыть DevTools

3. Перейдите на вкладку **Application** (или **Приложение**)

4. В левой панели найдите **Local Storage** → **http://localhost:5173**

5. Удалите следующие записи:
   - `user_email`
   - `user_name`

6. Обновите страницу (`F5` или `Ctrl+R`)

7. Введите корректный email и имя

---

### Способ 2: Через Console

1. Откройте frontend: http://localhost:5173

2. Нажмите `F12` чтобы открыть DevTools

3. Перейдите на вкладку **Console**

4. Выполните:
   ```javascript
   localStorage.clear()
   location.reload()
   ```

5. Введите корректный email и имя

---

### Способ 3: Через Incognito/Private

Откройте в режиме инкогнито:
- **Chrome:** `Ctrl+Shift+N`
- **Firefox:** `Ctrl+Shift+P`
- **Edge:** `Ctrl+Shift+N`

Затем откройте: http://localhost:5173

---

## ✅ Проверка правильного формата

### Корректный email:
- ✅ `user@company.com`
- ✅ `test@test.com`
- ✅ `name@example.org`

### Некорректный email:
- ❌ `user` (нет @ и домена)
- ❌ `user@` (нет домена)
- ❌ `@company.com` (нет имени)
- ❌ `user@company` (нет .com/.org/etc)

---

## 🐛 Если все еще не работает

### 1. Проверьте что backend запущен

Откройте: http://localhost:8000/health

Должно вернуть:
```json
{
  "status": "healthy",
  "database": "connected",
  "api": "operational"
}
```

### 2. Проверьте ANTHROPIC_API_KEY

В файле `backend\.env` должен быть реальный ключ:
```env
ANTHROPIC_API_KEY=sk-ant-api03-ваш_ключ
```

Не должно быть:
```env
ANTHROPIC_API_KEY=your_api_key_here  ❌
```

См. **FIX_API_KEY.md** для настройки ключа.

### 3. Проверьте логи backend

В терминале backend смотрите сообщения об ошибках:
- `422 Unprocessable Entity` - проблема валидации (неверный email)
- `AuthenticationError` - проблема с API ключом
- `CORS error` - проблема с ALLOWED_ORIGINS

---

## 📝 Формат данных для теста

После сброса введите:
- **Email:** `test@company.com`
- **Имя:** `Test User`

Затем протестируйте:
- Напишите: `Не работает VPN`
- AI должен создать тикет и предложить решение

---

**Время исправления:** 1 минута
**Сложность:** Очень легко ✅
