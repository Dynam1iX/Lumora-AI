# 🌐 Доступ к Lumora AI из интернета

## Быстрый старт: 3 способа открыть доступ

После запуска проекта локально, вы можете предоставить доступ из интернета тремя способами:

---

## 🚀 Способ 1: ngrok (РЕКОМЕНДУЕТСЯ)

**Что это:** Создает безопасный туннель к вашему localhost с HTTPS

### 1. Установка ngrok

**Windows:**
```bash
# Скачайте с https://ngrok.com/download
# Или через Chocolatey:
choco install ngrok
```

**Linux/Mac:**
```bash
# Скачайте с https://ngrok.com/download
# Или через Homebrew (Mac):
brew install ngrok/ngrok/ngrok
```

### 2. Регистрация (бесплатно)

1. Зайдите на https://dashboard.ngrok.com/signup
2. Зарегистрируйтесь (можно через GitHub)
3. Скопируйте ваш authtoken
4. Выполните:

```bash
ngrok config add-authtoken YOUR_AUTHTOKEN
```

### 3. Запустите туннели

**Терминал 1 - Backend туннель:**
```bash
ngrok http 8000
```

Вы получите URL типа: `https://abc123.ngrok-free.app`

**Терминал 2 - Frontend туннель:**
```bash
ngrok http 5173
```

Вы получите URL типа: `https://xyz789.ngrok-free.app`

### 4. Обновите конфигурацию

**backend/.env** - добавьте ngrok URL фронтенда в CORS:
```env
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,https://xyz789.ngrok-free.app
```

**frontend/.env** - укажите ngrok URL бэкенда:
```env
VITE_API_URL=https://abc123.ngrok-free.app
```

### 5. Перезапустите приложение

```bash
# Остановите backend (Ctrl+C) и запустите снова
cd backend
python main.py

# Остановите frontend (Ctrl+C) и запустите снова
cd frontend
npm run dev
```

### 6. Откройте в браузере

Откройте ngrok URL фронтенда: `https://xyz789.ngrok-free.app`

**✅ Готово!** Теперь приложение доступно из любой точки мира!

---

## 🔧 Способ 2: localtunnel

**Что это:** Простой туннель без регистрации

### 1. Установка

```bash
npm install -g localtunnel
```

### 2. Запустите туннели

**Терминал 1 - Backend:**
```bash
lt --port 8000 --subdomain lumora-backend
```

URL: `https://lumora-backend.loca.lt`

**Терминал 2 - Frontend:**
```bash
lt --port 5173 --subdomain lumora-frontend
```

URL: `https://lumora-frontend.loca.lt`

### 3. Обновите конфигурацию

**backend/.env:**
```env
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,https://lumora-frontend.loca.lt
```

**frontend/.env:**
```env
VITE_API_URL=https://lumora-backend.loca.lt
```

### 4. Перезапустите приложение

```bash
# Backend
cd backend
python main.py

# Frontend
cd frontend
npm run dev
```

**⚠️ Важно:** localtunnel может показать страницу предупреждения при первом посещении - просто нажмите "Continue"

---

## ☁️ Способ 3: Cloudflare Tunnel

**Что это:** Профессиональное решение от Cloudflare

### 1. Установка cloudflared

**Windows:**
```bash
# Скачайте с https://developers.cloudflare.com/cloudflare-one/connections/connect-apps/install-and-setup/installation/
```

**Linux:**
```bash
wget https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64
sudo mv cloudflared-linux-amd64 /usr/local/bin/cloudflared
sudo chmod +x /usr/local/bin/cloudflared
```

**Mac:**
```bash
brew install cloudflare/cloudflare/cloudflared
```

### 2. Регистрация

1. Зайдите на https://dash.cloudflare.com/
2. Зарегистрируйтесь или войдите
3. Выполните:

```bash
cloudflared tunnel login
```

### 3. Создайте туннель

```bash
cloudflared tunnel create lumora-ai
```

Вы получите tunnel ID - сохраните его!

### 4. Создайте конфиг

Создайте файл `~/.cloudflared/config.yml`:

```yaml
tunnel: YOUR_TUNNEL_ID
credentials-file: ~/.cloudflared/YOUR_TUNNEL_ID.json

ingress:
  # Backend
  - hostname: lumora-backend.yourdomain.com
    service: http://localhost:8000
  # Frontend
  - hostname: lumora-frontend.yourdomain.com
    service: http://localhost:5173
  # Catch-all
  - service: http_status:404
```

### 5. Запустите туннель

```bash
cloudflared tunnel run lumora-ai
```

### 6. Настройте DNS

В Cloudflare Dashboard добавьте CNAME записи:
- `lumora-backend` → `YOUR_TUNNEL_ID.cfargotunnel.com`
- `lumora-frontend` → `YOUR_TUNNEL_ID.cfargotunnel.com`

---

## 🎯 Сравнение способов

| Способ | Простота | Надежность | HTTPS | Бесплатно | Рекомендация |
|--------|----------|------------|-------|-----------|--------------|
| **ngrok** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ✅ | ✅ (с лимитами) | **Для хакатона** |
| **localtunnel** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ✅ | ✅ | Для быстрых тестов |
| **Cloudflare** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ✅ | ✅ | Для production |

---

## 🔒 Важные замечания о безопасности

### 1. Не используйте для критических данных
- Эти решения для разработки/демо
- Для production используйте полноценный хостинг (см. DEPLOY_NOW.md)

### 2. Защитите админ-панель
Смените пароль в `backend/.env`:
```env
ADMIN_USERNAME=admin
ADMIN_PASSWORD=YOUR_SECURE_PASSWORD_HERE
```

### 3. Ограничьте доступ
Можно добавить базовую аутентификацию в ngrok:
```bash
ngrok http 8000 --basic-auth="username:password"
```

---

## 🐛 Troubleshooting

### CORS ошибки
**Проблема:** Frontend не может подключиться к backend

**Решение:**
1. Проверьте `ALLOWED_ORIGINS` в `backend/.env`
2. Убедитесь что указали ТОЧНЫЙ URL из ngrok/localtunnel
3. Перезапустите backend

### ngrok: "Too many connections"
**Проблема:** Превышен лимит бесплатного плана

**Решение:**
1. Зарегистрируйтесь на ngrok.com (лимит увеличится)
2. Или используйте localtunnel как альтернативу

### localtunnel: "Connection refused"
**Проблема:** Туннель не может подключиться

**Решение:**
1. Убедитесь что приложение запущено на нужном порту
2. Попробуйте без `--subdomain` (случайный URL)
3. Или используйте ngrok

### Frontend показывает "Failed to fetch"
**Проблема:** Неверный API URL

**Решение:**
1. Проверьте `VITE_API_URL` в `frontend/.env`
2. Убедитесь что backend туннель работает
3. Проверьте что backend запущен: `curl BACKEND_URL/health`

---

## 📝 Быстрый чеклист для хакатона

### Вариант с ngrok (5 минут):

1. ✅ Установите ngrok
2. ✅ Зарегистрируйтесь и добавьте authtoken
3. ✅ Запустите проект локально (backend + frontend)
4. ✅ Откройте 2 терминала для ngrok туннелей
5. ✅ Обновите `.env` файлы с ngrok URLs
6. ✅ Перезапустите backend и frontend
7. ✅ Протестируйте через ngrok URL
8. ✅ Готово! 🎉

### Вариант с localtunnel (3 минуты):

1. ✅ Установите: `npm install -g localtunnel`
2. ✅ Запустите проект локально
3. ✅ Запустите туннели: `lt --port 8000` и `lt --port 5173`
4. ✅ Обновите `.env` файлы
5. ✅ Перезапустите приложение
6. ✅ Готово! 🎉

---

## 🚀 Следующие шаги

После тестирования через туннель:
- Если все работает → задеплойте на production (DEPLOY_NOW.md)
- Если нужны изменения → доработайте и протестируйте снова
- Покажите на хакатоне используя ngrok URL!

---

**Время настройки:** 3-5 минут
**Стоимость:** Бесплатно

Удачи на хакатоне! 🚀
