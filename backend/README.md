# IT HelpDesk Backend API

AI-powered IT HelpDesk система с интеграцией Claude API для автоматической помощи сотрудникам.

## Технологический стек

- **FastAPI** - современный веб-фреймворк для создания API
- **SQLAlchemy** - ORM для работы с базой данных
- **PostgreSQL / SQLite** - база данных (SQLite для разработки)
- **Anthropic Claude API** - AI помощник для ответов на вопросы
- **Pydantic** - валидация данных
- **Uvicorn** - ASGI сервер

## Функциональность

### Для пользователей
- 💬 **AI чат** - интеллектуальный помощник для решения IT проблем
- 📚 **База знаний** - поиск решений типовых проблем
- 🎫 **Создание тикетов** - если AI не смог помочь
- 📊 **Отслеживание тикетов** - проверка статуса заявки

### Для администраторов
- 📋 **Управление тикетами** - просмотр, обновление статуса, ответы
- 📝 **Управление базой знаний** - добавление новых решений
- 📈 **Статистика** - количество тикетов по статусам

## Установка и запуск

### 1. Создайте виртуальное окружение

```bash
cd backend
python -m venv venv

# Windows
venv\\Scripts\\activate

# Linux/Mac
source venv/bin/activate
```

### 2. Установите зависимости

```bash
pip install -r requirements.txt
```

### 3. Настройте переменные окружения

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Заполните переменные:

```env
DATABASE_URL=sqlite:///./helpdesk.db
ANTHROPIC_API_KEY=your_anthropic_api_key_here
CLAUDE_MODEL=claude-3-5-sonnet-20241022
DEBUG=True
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your_secure_password
```

**Важно:** Получите API ключ Anthropic на https://console.anthropic.com/

### 4. Инициализируйте базу данных

База данных создастся автоматически при первом запуске.

### 5. Заполните базу знаний

```bash
python seed_knowledge.py
```

Это добавит 15 типовых решений IT проблем.

### 6. Запустите сервер

```bash
# Способ 1: через uvicorn напрямую
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

# Способ 2: через main.py
python main.py
```

Сервер запустится на http://localhost:8000

## API Документация

После запуска сервера доступна интерактивная документация:

- **Swagger UI**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc

## API Endpoints

### Публичные endpoints

#### Chat
- `POST /api/chat` - отправить сообщение AI помощнику

```json
{
  "session_id": "unique-session-id",
  "message": "Не могу войти в корпоративную почту",
  "category": "access_passwords"
}
```

#### Tickets
- `POST /api/tickets` - создать новый тикет
- `GET /api/tickets/{id}` - получить тикет по ID

#### Knowledge Base
- `GET /api/knowledge/search?q=vpn&category=network` - поиск в базе знаний
- `GET /api/knowledge/{id}` - получить статью по ID

### Admin endpoints (требуют аутентификации)

Передавайте `username` и `password` как query параметры:

- `GET /api/admin/tickets?username=admin&password=xxx` - список всех тикетов
- `PATCH /api/admin/tickets/{id}?username=admin&password=xxx` - обновить тикет
- `POST /api/admin/knowledge?username=admin&password=xxx` - добавить статью
- `GET /api/admin/knowledge?username=admin&password=xxx` - список всех статей
- `GET /api/admin/stats?username=admin&password=xxx` - статистика

## Структура проекта

```
backend/
├── app/
│   ├── core/           # Конфигурация, база данных
│   ├── models/         # SQLAlchemy модели
│   ├── schemas/        # Pydantic схемы
│   ├── services/       # Бизнес-логика
│   ├── routers/        # API endpoints
│   └── main.py         # FastAPI приложение
├── main.py             # Точка входа
├── requirements.txt    # Зависимости
└── seed_knowledge.py   # Скрипт наполнения БЗ
```

## Модели данных

### Ticket (Тикет)
- id, user_name, email
- category (категория проблемы)
- problem (описание)
- status (new/in_progress/resolved/closed)
- admin_response
- created_at, updated_at

### KnowledgeBase (База знаний)
- id, category
- title (заголовок)
- problem (описание проблемы)
- solution (решение)
- keywords (ключевые слова)

### ChatHistory (История чата)
- id, session_id
- message, is_bot
- confidence_score
- created_at

## Категории проблем

- `access_passwords` - Доступы и пароли
- `network` - Сеть и интернет
- `printers` - Принтеры
- `software` - Программное обеспечение
- `hardware` - Оборудование
- `other` - Прочее

## AI Логика

AI помощник (Claude):
1. Получает вопрос пользователя и историю чата
2. Ищет релевантные статьи в базе знаний
3. Формирует ответ на основе найденной информации
4. Возвращает ответ и confidence score (0.0-1.0)
5. Если confidence < 0.6, рекомендует создать тикет

## Разработка

### Миграции базы данных

Для production рекомендуется использовать Alembic:

```bash
# Создать миграцию
alembic revision --autogenerate -m "description"

# Применить миграции
alembic upgrade head
```

### Тестирование

```bash
# Установите pytest
pip install pytest pytest-asyncio httpx

# Запустите тесты
pytest
```

## Production Deploy

### Railway / Render

1. Создайте PostgreSQL базу данных
2. Установите переменные окружения
3. Деплойте через Git или Docker

### Docker

```dockerfile
FROM python:3.11-slim

WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
```

## Troubleshooting

### Ошибка подключения к БД
- Проверьте правильность DATABASE_URL
- Убедитесь что PostgreSQL запущен (если используете)

### AI не отвечает
- Проверьте что ANTHROPIC_API_KEY корректный
- Проверьте лимиты использования API
- Проверьте логи сервера

### CORS ошибки
- Добавьте URL фронтенда в ALLOWED_ORIGINS

## Лицензия

MIT License

## Команда

MayhemMonkeys - Hackathon Project 2024
