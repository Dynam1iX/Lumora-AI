# ▶️ ЗАПУСК СЕЙЧАС - Пошаговая инструкция

## Шаг 1: Проверка окружения

### Откройте CMD или PowerShell

Проверьте что установлено:

```bash
python --version
# Должно быть: Python 3.11 или выше

node --version
# Должно быть: Node.js 18 или выше

npm --version
# Должно быть: npm 8 или выше
```

---

## Шаг 2: Настройка Backend

### 2.1 Откройте терминал в папке проекта

```bash
cd C:\Users\Est1ege\Documents\GitHub\Lumora-AI
```

### 2.2 Перейдите в backend

```bash
cd backend
```

### 2.3 Создайте виртуальное окружение

```bash
python -m venv venv
```

### 2.4 Активируйте виртуальное окружение

**Windows CMD:**
```bash
venv\Scripts\activate
```

**Windows PowerShell:**
```powershell
venv\Scripts\Activate.ps1
```

Если PowerShell ругается на политику выполнения:
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### 2.5 Установите зависимости

```bash
pip install -r requirements.txt
```

### 2.6 Проверьте .env файл

Откройте `backend/.env` и убедитесь что есть:

```env
ANTHROPIC_API_KEY=ваш_реальный_ключ
```

**Получить ключ:** https://console.anthropic.com/settings/keys

### 2.7 Заполните базу знаний

```bash
python seed_knowledge.py
```

Вы должны увидеть: "✅ Successfully added 15 knowledge articles"

### 2.8 Запустите Backend

```bash
python main.py
```

Вы должны увидеть:
```
INFO:     Uvicorn running on http://127.0.0.1:8000 (Press CTRL+C to quit)
```

**✅ Backend запущен!**

Проверьте в браузере: http://localhost:8000/health

Должно вернуть:
```json
{
  "status": "healthy",
  "database": "connected",
  "api": "operational"
}
```

---

## Шаг 3: Настройка Frontend

### 3.1 Откройте НОВЫЙ терминал (не закрывайте backend!)

```bash
cd C:\Users\Est1ege\Documents\GitHub\Lumora-AI
cd frontend
```

### 3.2 Установите зависимости (первый раз)

```bash
npm install
```

Подождите 1-2 минуты...

### 3.3 Проверьте .env файл

Убедитесь что `frontend/.env` содержит:

```env
VITE_API_URL=http://localhost:8000
```

### 3.4 Запустите Frontend

```bash
npm run dev
```

Вы должны увидеть:
```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

**✅ Frontend запущен!**

---

## Шаг 4: Тест работы

### 4.1 Откройте браузер

Перейдите на: http://localhost:5173

### 4.2 Протестируйте чат

1. При первом запуске введите:
   - **Имя:** Ваше имя
   - **Email:** test@example.com

2. Напишите в чат:
   ```
   Не работает VPN
   ```

3. AI должен:
   - Создать тикет
   - Определить категорию и приоритет
   - Предложить решение

### 4.3 Проверьте что тикет создался

Откройте API документацию: http://localhost:8000/docs

Или проверьте напрямую через curl:

```bash
curl http://localhost:8000/api/tickets
```

**✅ Если видите тикет - все работает!**

---

## Шаг 5: Открытие доступа из интернета

### Вариант A: ngrok (рекомендуется)

**5.A.1 Скачайте ngrok**

https://ngrok.com/download

Распакуйте в удобную папку (например: `C:\ngrok`)

**5.A.2 Зарегистрируйтесь**

https://dashboard.ngrok.com/signup

Скопируйте authtoken

**5.A.3 Добавьте authtoken**

```bash
C:\ngrok\ngrok.exe config add-authtoken YOUR_AUTHTOKEN
```

**5.A.4 Откройте 2 НОВЫХ терминала для туннелей**

**Терминал 3 (Backend туннель):**
```bash
C:\ngrok\ngrok.exe http 8000
```

Скопируйте URL типа: `https://abc123.ngrok-free.app`

**Терминал 4 (Frontend туннель):**
```bash
C:\ngrok\ngrok.exe http 5173
```

Скопируйте URL типа: `https://xyz789.ngrok-free.app`

**5.A.5 Обновите конфигурацию**

Откройте `backend\.env` и измените:

```env
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,https://xyz789.ngrok-free.app
```

Где `https://xyz789.ngrok-free.app` - ваш frontend ngrok URL

Откройте `frontend\.env` и измените:

```env
VITE_API_URL=https://abc123.ngrok-free.app
```

Где `https://abc123.ngrok-free.app` - ваш backend ngrok URL

**5.A.6 Перезапустите приложение**

**Backend терминал (Терминал 1):**
- Нажмите `Ctrl+C`
- Запустите снова: `python main.py`

**Frontend терминал (Терминал 2):**
- Нажмите `Ctrl+C`
- Запустите снова: `npm run dev`

**5.A.7 Откройте ngrok URL в браузере**

Откройте ваш **frontend ngrok URL** (из Терминала 4):
```
https://xyz789.ngrok-free.app
```

**✅ Готово! Приложение доступно из интернета!**

Можете поделиться этим URL с кем угодно!

---

### Вариант B: localtunnel (проще, но менее стабильно)

**5.B.1 Установите localtunnel**

```bash
npm install -g localtunnel
```

**5.B.2 Запустите туннели**

**Терминал 3 (Backend):**
```bash
lt --port 8000 --subdomain lumora-backend
```

URL: `https://lumora-backend.loca.lt`

**Терминал 4 (Frontend):**
```bash
lt --port 5173 --subdomain lumora-frontend
```

URL: `https://lumora-frontend.loca.lt`

**5.B.3 Обновите конфигурацию**

`backend\.env`:
```env
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,https://lumora-frontend.loca.lt
```

`frontend\.env`:
```env
VITE_API_URL=https://lumora-backend.loca.lt
```

**5.B.4 Перезапустите приложение**

Backend: `Ctrl+C` → `python main.py`
Frontend: `Ctrl+C` → `npm run dev`

**5.B.5 Откройте в браузере**

```
https://lumora-frontend.loca.lt
```

При первом посещении нажмите "Continue"

---

## 🎯 Итоговая конфигурация

У вас должно быть **4 открытых терминала**:

1. **Терминал 1:** Backend (python main.py)
2. **Терминал 2:** Frontend (npm run dev)
3. **Терминал 3:** Backend туннель (ngrok http 8000)
4. **Терминал 4:** Frontend туннель (ngrok http 5173)

---

## 🐛 Частые проблемы

### Backend не запускается

**Ошибка:** `ModuleNotFoundError`

**Решение:**
```bash
cd backend
pip install -r requirements.txt
```

**Ошибка:** `ANTHROPIC_API_KEY not found`

**Решение:**
Добавьте реальный ключ в `backend\.env`

### Frontend не подключается к Backend

**Ошибка:** CORS или "Failed to fetch"

**Решение:**
1. Проверьте `VITE_API_URL` в `frontend\.env`
2. Убедитесь что backend запущен: http://localhost:8000/health
3. Проверьте `ALLOWED_ORIGINS` в `backend\.env`

### ngrok туннель не работает

**Ошибка:** "Tunnel not found"

**Решение:**
1. Убедитесь что добавили authtoken: `ngrok config add-authtoken YOUR_TOKEN`
2. Проверьте что приложение запущено на нужном порту

---

## 📋 Быстрый чеклист

### Локальный запуск:
- [ ] Backend запущен (http://localhost:8000)
- [ ] Frontend запущен (http://localhost:5173)
- [ ] Health check работает (http://localhost:8000/health)
- [ ] Чат работает (можно создать тикет)

### Доступ из сети:
- [ ] ngrok/localtunnel установлен
- [ ] Туннели запущены (2 терминала)
- [ ] `.env` файлы обновлены с ngrok URLs
- [ ] Приложение перезапущено
- [ ] Работает через ngrok URL

---

## 🚀 Готово!

Теперь у вас:
- ✅ Локально работающее приложение
- ✅ Доступ из интернета через ngrok/localtunnel
- ✅ Готово к демо на хакатоне!

**Следующие шаги:**
- Тестируйте функционал
- Покажите на хакатоне через ngrok URL
- Если нужен постоянный хостинг → см. DEPLOY_NOW.md

Удачи! 🎉
