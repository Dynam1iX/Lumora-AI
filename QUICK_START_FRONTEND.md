# Quick Start для Frontend разработчика

## 🎯 Что нужно знать

**Backend полностью готов!** Вам нужен только ОДИН endpoint.

### Главный endpoint: POST /api/support

Отправляете:
```json
{
  "user_name": "Имя пользователя",
  "email": "email@example.com",
  "problem": "Текст проблемы"
}
```

Получаете:
```json
{
  "id": 123,
  "status": "ai_resolved" или "needs_human",
  "category": "network",
  "priority": "high",
  "ai_solution": "Пошаговое решение...",
  "ai_confidence": 0.85,
  ...
}
```

---

## 🚀 Быстрый старт

### 1. Запустите backend

```bash
cd backend
python main.py
```

Backend запустится на http://localhost:8000

### 2. Протестируйте API

Откройте http://localhost:8000/docs - там интерактивная документация!

Или через curl:
```bash
curl -X POST http://localhost:8000/api/support \
  -H "Content-Type: application/json" \
  -d '{
    "user_name": "Тест",
    "email": "test@test.com",
    "problem": "Не работает VPN"
  }'
```

---

## 💻 Минимальный код для интеграции

### React пример

```tsx
import { useState } from 'react'

function SupportPage() {
  const [problem, setProblem] = useState('')
  const [ticket, setTicket] = useState(null)
  const [loading, setLoading] = useState(false)

  const handleSubmit = async (e) => {
    e.preventDefault()
    setLoading(true)

    const response = await fetch('http://localhost:8000/api/support', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        user_name: 'User Name',  // Из вашей auth системы
        email: 'user@company.com',
        problem: problem
      })
    })

    const data = await response.json()
    setTicket(data)
    setLoading(false)
  }

  return (
    <div>
      {!ticket ? (
        <form onSubmit={handleSubmit}>
          <textarea
            value={problem}
            onChange={(e) => setProblem(e.target.value)}
            placeholder="Опишите вашу проблему..."
          />
          <button disabled={loading}>
            {loading ? 'Обработка...' : 'Отправить'}
          </button>
        </form>
      ) : (
        <div>
          {ticket.status === 'ai_resolved' ? (
            <div className="success">
              <h3>✅ Проблема решена автоматически!</h3>
              <p><strong>Категория:</strong> {ticket.category}</p>
              <p><strong>Приоритет:</strong> {ticket.priority}</p>
              <div>
                <h4>Решение:</h4>
                <pre>{ticket.ai_solution}</pre>
              </div>
              <button>Решение помогло ✓</button>
              <button>Не помогло ✗</button>
            </div>
          ) : (
            <div className="pending">
              <h3>⏳ Тикет передан специалисту</h3>
              <p>Тикет #{ticket.id}</p>
              <p>Ожидайте ответ в течение 2-4 часов</p>
              {ticket.ai_solution && (
                <div>
                  <p>Пока вы ждете, попробуйте:</p>
                  <pre>{ticket.ai_solution}</pre>
                </div>
              )}
            </div>
          )}
        </div>
      )}
    </div>
  )
}
```

---

## 📋 Что отображать пользователю

### Если status = "ai_resolved" (AI решил)

```
┌─────────────────────────────────────┐
│ ✅ Проблема решена автоматически!   │
│                                     │
│ Категория: Сеть и интернет         │
│ Приоритет: Высокий                 │
│ Уверенность AI: 85%                │
│                                     │
│ Решение:                            │
│ 1. Проверьте интернет              │
│ 2. Перезапустите VPN клиент        │
│ 3. ...                             │
│                                     │
│ [Решение помогло ✓] [Не помогло ✗] │
└─────────────────────────────────────┘
```

### Если status = "needs_human" (нужен специалист)

```
┌─────────────────────────────────────┐
│ ⏳ Тикет передан специалисту       │
│                                     │
│ Тикет #1234                        │
│ Категория: Оборудование            │
│ Приоритет: Критический             │
│                                     │
│ Ожидаемое время ответа: 2-4 часа   │
│                                     │
│ [Проверить статус]                 │
└─────────────────────────────────────┘
```

---

## 🎨 Константы для UI

```typescript
// Статусы
const STATUS_LABELS = {
  ai_resolved: 'Решено автоматически',
  needs_human: 'Ожидает специалиста',
  in_progress: 'В работе',
  resolved: 'Решено',
  closed: 'Закрыто'
}

// Приоритеты (с цветами)
const PRIORITIES = {
  low: { label: 'Низкий', color: '#10b981', icon: '🟢' },
  medium: { label: 'Средний', color: '#f59e0b', icon: '🟡' },
  high: { label: 'Высокий', color: '#ef4444', icon: '🔴' },
  critical: { label: 'Критический', color: '#991b1b', icon: '🚨' }
}

// Категории
const CATEGORIES = {
  access_passwords: 'Доступы и пароли',
  network: 'Сеть и интернет',
  printers: 'Принтеры',
  software: 'Программное обеспечение',
  hardware: 'Оборудование',
  other: 'Прочее'
}
```

---

## 🔄 Получить тикеты пользователя

```typescript
const getMyTickets = async (email: string) => {
  const response = await fetch(
    `http://localhost:8000/api/support/my-tickets?email=${email}`
  )
  const tickets = await response.json()
  return tickets
}

// tickets - массив всех тикетов пользователя
```

---

## ⚡ Это ВСЁ что нужно!

Серьезно, вам нужны только:
1. **POST /api/support** - создать обращение
2. **GET /api/support/my-tickets?email=...** - получить тикеты

Backend делает ВСЮ магию:
- ✅ Создает тикет
- ✅ Определяет категорию
- ✅ Определяет приоритет
- ✅ Пытается решить
- ✅ Решает эскалировать или нет

---

## 📚 Дополнительная документация

- **FRONTEND_INTEGRATION.md** - полное руководство с примерами
- **CHANGELOG_V2.md** - что изменилось в v2.0
- **http://localhost:8000/docs** - API документация (Swagger)

---

## 🐛 Отладка

### Backend не запускается?

```bash
cd backend
python -m venv venv
venv\Scripts\activate  # Windows
pip install -r requirements.txt
```

### Ошибка "ANTHROPIC_API_KEY not found"?

Отредактируйте `backend/.env`:
```
ANTHROPIC_API_KEY=ваш_ключ_здесь
```

### CORS ошибки?

Добавьте ваш домен в `backend/.env`:
```
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173
```

---

## ✅ Checklist интеграции

- [ ] Backend запущен и работает
- [ ] Тест POST /api/support через Swagger
- [ ] Форма для ввода проблемы
- [ ] Отправка на /api/support
- [ ] Показ AI решения (status = ai_resolved)
- [ ] Показ "ожидайте специалиста" (status = needs_human)
- [ ] Страница "Мои тикеты"
- [ ] Обработка ошибок

---

**Готово! Начинайте интегрировать.** 🚀

Вопросы? Смотрите Swagger docs: http://localhost:8000/docs
