# Lumora AI - Frontend

Фронтенд для AI HelpDesk системы с чат-ботом поддержки.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

### 2. Запуск dev сервера

```bash
npm run dev
```

Приложение откроется на http://localhost:5173

### 3. Сборка для production

```bash
npm run build
```

## 📁 Структура проекта

```
src/
├── components/
│   ├── icons/              # SVG иконки
│   ├── ChatBot.tsx         # Главный компонент чата
│   ├── ChatHeader.tsx      # Заголовок чата
│   ├── ChatMessage.tsx     # Компонент сообщения
│   ├── ChatInput.tsx       # Поле ввода
│   └── Header.tsx          # Шапка приложения
├── services/
│   └── api.ts              # API calls
├── types/
│   └── index.ts            # TypeScript типы
├── App.tsx                 # Главный компонент
├── main.tsx                # Точка входа
└── index.css               # Tailwind стили
```

## 🎨 Технологии

- **React 18** - UI библиотека
- **TypeScript** - типизация
- **Vite** - сборщик
- **Tailwind CSS** - стилизация
- **Axios** - HTTP клиент

## 🔌 Подключение к Backend

Backend должен быть запущен на `http://localhost:8000`

API endpoints (из backend/README.md):
- `POST /api/chat` - отправить сообщение боту
- `POST /api/tickets` - создать тикет
- `GET /api/knowledge/search?q=...` - поиск в базе знаний

### Интеграция API

В файле `src/services/api.ts` уже настроены все методы:

```typescript
import { chatService } from './services/api';

// Отправка сообщения
const response = await chatService.sendMessage({
  session_id: 'unique-session-id',
  message: 'Мой вопрос',
  category: 'network' // опционально
});
```

## 🎨 Дизайн

Дизайн полностью соответствует скриншоту:

- **Темная тема** - #0a0e1a фон
- **Header** - логотип + 3 кнопки навигации
- **Чат** - аватар бота, сообщения, анимации
- **Адаптивность** - от мобильных до desktop

## 📱 Адаптивность

✅ Desktop (1024px+)  
✅ Tablet (768px+)  
✅ Mobile (320px+)

## 🔧 Кастомизация

### Изменить цвета

В `tailwind.config.js`:

```javascript
colors: {
  dark: {
    bg: '#0a0e1a',        // Основной фон
    secondary: '#14181f',  // Фон чата
    tertiary: '#1f2937',   // Фон сообщений
    border: '#374151',     // Границы
  },
}
```

### Изменить API URL

Создайте файл `.env`:

```
VITE_API_URL=http://localhost:8000/api
```

## 🐛 Troubleshooting

### Ошибка при npm install
```bash
# Очистите кэш
npm cache clean --force
rm -rf node_modules package-lock.json
npm install
```

### Backend не отвечает
- Проверьте что backend запущен на порту 8000
- Проверьте CORS настройки в backend
- Откройте DevTools (F12) → Network для отладки

### Стили не применяются
```bash
# Пересоберите Tailwind
npm run dev
```

## 📝 TODO

- [ ] Роутинг (React Router)
- [ ] Страница базы знаний
- [ ] Админ-панель
- [ ] Форма создания тикета
- [ ] Уведомления (toast)
- [ ] Темная/светлая тема

## 🤝 Интеграция с Backend

Когда backend будет готов, замените моковые данные на реальные API calls:

В `src/components/ChatBot.tsx` раскомментируйте:

```typescript
const response = await chatService.sendMessage({
  session_id: 'unique-session-id',
  message: text,
});

const botMessage: Message = {
  id: (Date.now() + 1).toString(),
  type: 'bot',
  text: response.response,
  timestamp: new Date().toLocaleTimeString('ru-RU', { hour: '2-digit', minute: '2-digit' }),
  confidence: response.confidence_score,
};
```

---

**Готово к работе!** 🚀

Просто запустите `npm install && npm run dev`
